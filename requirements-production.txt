# Production Requirements for FraudDetectPro
# Minimal dependencies for API deployment (~1-2GB instead of 8GB)

# Core API Framework
fastapi==0.118.0
uvicorn==0.37.0
pydantic==2.11.9
pydantic_core==2.33.2
starlette==0.48.0

# Firebase Authentication
firebase-admin==6.5.0

# Machine Learning Core
scikit-learn==1.7.2
xgboost==3.0.5
joblib==1.5.2
numpy>=1.23.5,<2.0.0  # TensorFlow 2.15.0 requires numpy < 2.0.0

# TensorFlow (for neural network feature extraction)
# Using CPU-only version to reduce size (~500MB vs ~2GB)
tensorflow-cpu==2.15.0

# SHAP (for explainability)
shap==0.48.0

# HTTP Client
requests==2.32.5

# Authentication
python-jose==3.5.0
python-multipart==0.0.20

# Core dependencies (auto-installed but listed for clarity)
anyio==4.11.0
click==8.3.0
h11==0.16.0
idna==3.10
sniffio==1.3.1
typing_extensions==4.15.0
urllib3==2.5.0

# Security
cryptography==46.0.1
bcrypt==4.0.1

# Utilities
python-dateutil==2.9.0.post0
pytz==2025.2

